import { Navigate } from "react-router-dom";

// Diese Seite wurde in WithdrawalPlan.tsx integriert.
export default function WithdrawalPlanDetail() {
  return <Navigate to="/withdrawal-plan" replace />;
}
